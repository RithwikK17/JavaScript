// Non-primitive data types
var users = ["john","tom","harry","mary"];

console.log('users =', users);
console.log('users =', typeof users); //object
console.log('users =', users.lenght);
document.writeln(`<h1> users = ${users} </h1>`);
document.writeln(`<h1> users = ${users[2]}</h1>`);

// destructure is the process of passing the data to the console and to find the prototype of the datatype (applicable only to non-primitive type)
// iteration is the process of accessing the elements one by one
// primitive does not have prototypes only the nn primitive has prototype
